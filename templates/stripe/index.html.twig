{% extends '@CapCommercio/base.html.twig' %}

{% block body %}

    <div class="card">
        <h3 class="card-header">Stripe</h3>

        <div class="card-body">

            <ul class="list-group">
                <li class="list-group-item">
                    <a href="{{ path('stripe_payment_index') }}">Les clients</a>
                </li>
                <li class="list-group-item">
                    <a href="{{ path('stripe_customer_index') }}">Les paiements</a>
                </li>
            </ul>
        </div>
    </div>

{% endblock %}
