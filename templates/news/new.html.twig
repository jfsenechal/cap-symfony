{% extends '@CapCommercio/base.html.twig' %}

{% block body %}

    <div class="card">
        <h3 class="card-header">Nouvelle news</h3>

        <div class="card-body">
            {% include '@CapCommercio/news/_form.html.twig' %}
        </div>
        <div class="card-footer text-end">
            <a href="{{ path('cap_news_index') }}" class="btn btn-secondary">
                <i class="fa fa-arrow-left"></i> Annuler
            </a>
        </div>
    </div>

{% endblock %}
