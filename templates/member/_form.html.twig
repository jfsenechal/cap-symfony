{{ form_start(form) }}
{{ form_errors(form) }}

<h4 class="text-success">Infos de facturation</h4>
{{ form_row(form.legalEntity) }}

<div class="row">
    <div class="col-6">{{ form_row(form.vatNumber) }}</div>
    <div class="col-6">{{ form_row(form.address.street1) }}</div>
</div>
<div class="row">
    <div class="col-6">{{ form_row(form.address.zipcode) }}</div>
    <div class="col-6">{{ form_row(form.address.city) }}</div>
</div>
<h4 class="text-success">Informations légales</h4>
<div class="row">
    <div class="col-6">{{ form_row(form.legalLastname) }}</div>
    <div class="col-6">{{ form_row(form.legalFirstname) }}</div>
</div>
<div class="row">
    <div class="col-6">{{ form_row(form.legalEmail) }}</div>
    <div class="col-6">{{ form_row(form.legalEmail2) }}</div>
</div>
<div class="row">
    <div class="col-6">{{ form_row(form.affiliationDate) }}</div>
    <div class="col-6">{{ form_row(form.legalPhone) }}</div>
</div>

<button class="btn btn-primary"><i class="fa fa-save"></i> Sauvegarder</button>
{{ form_end(form) }}
