{% extends '@CapCommercio/base.html.twig' %}

{% block body %}

    <div class="card">
        <h3 class="card-header">Nouvelle catégorie</h3>

        <div class="card-body">
            {% include '@CapCommercio/blog_category/_form.html.twig' %}
        </div>
        <div class="card-footer text-end">
            <a href="{{ path('cap_blog_category_index') }}" class="btn btn-secondary">
                <i class="ti ti-arrow-left"></i> Annuler
            </a>
        </div>
    </div>

{% endblock %}
