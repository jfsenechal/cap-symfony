{% extends '@CapCommercio/base.html.twig' %}

{% block body %}

    <div class="card">
        <h3 class="card-header">{{ category.name }}</h3>

        <div class="card-body">
            {% include '@CapCommercio/blog_category/_form.html.twig' %}
        </div>
        <div class="card-footer text-end">
            <a href="{{ path('cap_blog_category_show', {'id': category.id}) }}" class="btn btn-secondary">
                <i class="ti ti-arrow-left"></i> Annuler
            </a>
        </div>
    </div>

{% endblock %}
