{% extends '@CapCommercio/base.html.twig' %}

{% block body %}
    <div class="card">
        <h3 class="card-header">{{ order.orderNumber }}</h3>

        <div class="card-body">

            <table class="table table-bordered">
                <tr>
                    <th>Commercant</th>
                    <td>{{ order.orderCommercant.companyName }}</td>
                </tr>
            </table>
            <h4>Commerçant</h4>
            {{ dump(orderCommercant) }}

            <h4>Lignes</h4>
            {% for line in lines %}
                {{ dump(line) }}
            {% endfor %}

            <h4>Addresses</h4>
            {% for address in addresses %}
                {{ dump(address) }}
            {% endfor %}

            <h4>Bill</h4>

            {{ dump(bill) }}


            <a href="{{ path('cap_order_delete', {'id': order.id}) }}" class="btn btn-danger">
                Delete
            </a>
        </div>
    </div>
{% endblock %}

