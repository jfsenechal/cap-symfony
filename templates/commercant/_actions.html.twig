<div class="row gy-2 gx-1 align-items-center d-print-none mb-3">
    <div class="col-auto">
        <a href="{{ path('cap_commercant_member',{'id':commercant.id}) }}" class="btn btn-primary" role='button'>
            <i class="fas fa-people-group"></i>
            Membre ?
        </a>
    </div>
    <div class="col-auto">
        <a href="{{ path('cap_commercant_edit',{'id':commercant.id}) }}" class="btn btn-primary" role='button'>
            <i class="fas fa-edit"></i>
            Modifier
        </a>
    </div>
    {% if commercant.rightAccess %}
        <div class="col-auto">
            <a href="{{ path('cap_user_show',{'id':commercant.rightAccess.id}) }}" class="btn btn-primary" role='button'>
                <i class="fas fa-people-group"></i>
                Compte
            </a>
        </div>
    {% endif %}
</div>

