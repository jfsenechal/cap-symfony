<form method="post" action="{{ path('cap_commercant_delete', {'id': commercant.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commercant.id) }}">
    <button class="btn">Delete</button>
</form>
